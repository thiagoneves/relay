<!-- relay -->
Before starting, read `.relay/project.md`. Rules, gotchas and decisions live under `.relay/`, one file each; a handoff someone shared is in `.relay/handoffs/`. Save something with `relay remember decision|gotcha|rule "<one line>"` only when it would change what the next session does and cannot be read from the code: a trap you fell into, a choice that was settled, a rule the code does not show. Not general principles, not what a linter or a config file already says.
<!-- /relay -->
